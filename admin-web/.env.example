# Copier en .env.local (non versionné) si le backend n'est pas sur http://localhost:3000.
# L'adresse reste modifiable à chaud depuis l'onglet Paramètres de l'interface.
VITE_API_URL=http://localhost:3000
